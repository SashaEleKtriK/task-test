Сервис парсит XML - динамически создает и заполняет таблицы.
При создании таблицы создаются связи, поэтому важно создавать их в правильном порядке.

Запуск:
- в терминале перейти в папку \task-test
- выполнить команду: docker-compose up
После запуска докер контейнеров:
- открыть новый терминал
- выполнить команду: docker attach parser-app
- ввести help для получения справки